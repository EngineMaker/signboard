# 依頼：シェアハウスのリビング用「電光掲示板」Webアプリ

## 背景
- シェアハウスのリビングにiPadを置き、お知らせを電光掲示板のように横スクロールで流したい
- 市販のLEDバナーアプリはお知らせを変えるたびにiPad本体を操作する必要があり、常時表示用途に向かない
- そこで、iPadのSafariで「ホーム画面に追加」して全画面表示するWebページを作り、内容はリモートから更新したい
- 住人が自分のスマホからお知らせを投稿できるようにする案もある（採用するかは要件次第）

## 進め方（厳守）
いきなり実装しないこと。以下のフェーズを順番に進め、各フェーズの終わりで私の承認を得てから次へ進むこと。

### フェーズ1：要件ヒアリング
- 質問は一度に3〜5個までにし、何往復かに分けて聞くこと
- 最低限、次の点を確認すること：
  - 誰が投稿・編集できるか（私だけか、住人全員か）。認証の要否
  - お知らせの寿命（期限切れで自動的に消すかどうか）
  - 表示の要件（複数件のローテーション、色分けや優先度、時計・日付の表示など）
  - ホスティング先と運用コストの上限（無料枠で収めたいかどうか）
  - iPadの機種とiPadOSのバージョン
  - ネットワーク切断時の挙動（最後に取得した内容を表示し続けるかどうか）
- ヒアリング結果を `docs/SPEC.md` にまとめ、承認を得ること

### フェーズ2：開発手法と技術選定の提案
- 「AIが実装し、人間がレビューする」ことを前提に、手法を提案すること。以下を満たすこと：
  - 小さな縦切りのステップに分け、1ステップごとに動作確認できる状態にする
  - 各ステップに、機械的に検証できる受け入れ条件を付ける（テスト、またはコマンドで確認できる形）
  - 仕様・計画・決定事項をリポジトリ内のファイルに残す。セッションをまたいでも文脈が失われないようにするため
  - 私がレビューすべきポイントを明示する
- 技術スタックは選択肢を2〜3案出し、トレードオフを簡潔に示すこと。過剰な構成は避ける
- 結果を `docs/PLAN.md` にまとめ、承認を得ること

### フェーズ3：実装
- PLAN.mdのステップを1つずつ実装すること
- 各ステップの完了時に、受け入れ条件の確認結果を報告し、PLAN.mdの進捗を更新すること
- 計画から外れる判断が必要になったら、実装を止めて相談すること

## 制約
- 私はプログラマ歴が長いので、技術的な説明は簡潔でよい
- 報告は短くすること。一度に大量に出力しないこと
